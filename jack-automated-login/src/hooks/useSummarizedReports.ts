import { useQuery } from "@chakra-ui/react";
import axios from "axios";
import { SummarizedReportsType } from "../schemes/SummarizedReports.scheme.ts";

export default function useSummarizedReports() {
  // const getApiUrl = `https://`;
  // return useQuery({
  //   queryKey: ["summarized_reports"],
  //   queryFn: () => axios.get<SummarizedReportsType[]>,
  // });
}
